# 中国服务器优化版依赖文件
# 使用国内镜像加速安装：pip install -r requirements_china.txt -i https://mirrors.aliyun.com/pypi/simple/

# Web框架
Flask==3.0.0
Flask-CORS==4.0.0

# 任务调度
APScheduler==3.10.4

# 无头浏览器（反爬虫绕过）
playwright==1.40.0

# HTTP客户端（支持代理）
aiohttp==3.9.1
aiohttp-socks==0.8.4  # SOCKS代理支持
python-socks[asyncio]==2.4.3  # SOCKS代理依赖
requests==2.31.0
pysocks==1.7.1  # SOCKS代理支持

# 数据库
# SQLite已内置，无需额外安装

# 日志
colorlog==6.8.0

# 工具库
python-dotenv==1.0.0

# 系统监控（长时间运行优化）
psutil==5.9.6

# 进程管理
gunicorn==21.2.0
